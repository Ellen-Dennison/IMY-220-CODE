services:
  backend:
    build: ./backend
    ports:
      - '4000:4000'
    environment:
      - PORT=4000

  frontend:
    build: ./frontend
    ports:
      - '5173:5173'
    environment:
      - BACKEND_URL=http://backend:4000
    depends_on:
      - backend
